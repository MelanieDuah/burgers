{{#each burgers}}

{{#if this.devoured}}
<div class="row">
    <div class="col-md-12">
        <div class="col-auto">
            <p class="mt-3">{{this.burger_name}}</p>
        </div>
    </div>
</div>
{{/if}}
{{/each}}