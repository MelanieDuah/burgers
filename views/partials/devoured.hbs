{{#each burgers}}

{{#if this.devoured}}
<div class="row mt-1">
        <div class="col-md-12 burger">
            <p class="mt-3 text-dark">{{this.burger_name}}</p>
        </div>
    </div>
{{/if}}
{{/each}}